export interface Icart {
  productID: number;
  productName: string;
  productPrice: number;
  productSize: string;
  productColor: string;
  productOrderQuantity: number;
  productImageName: string;
}
